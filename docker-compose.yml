version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: leboncoin-app
    ports:
      - "8080:8080"
    environment:
      - APP_ENV=local
      - APP_DEBUG=true
      - APP_KEY=base64:DVA6dRRyXh6h3ZVSzSXJO+xwUXX8dAvQKlBsGyPVUvQ=
      - PORT=8080
      - DB_CONNECTION=sqlite
      - CACHE_STORE=database
      - SESSION_DRIVER=database
    networks:
      - leboncoin-network
    restart: unless-stopped

networks:
  leboncoin-network:
    driver: bridge
